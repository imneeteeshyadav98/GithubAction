import { makeStyles } from "@material-ui/core";

const useStyles = makeStyles((theme) => ({
  sideBanner: {
    height: "100vh",
    background: "linear-gradient(to top, #62667D 0%, #1A173E 100%)",
    display:"flex",
    justifyContent:"center",
    alignItems:"center",
  },
}));

export default useStyles;
